/** @type {import('next').NextConfig} */
const nextConfig = {
  devIndicators: {
    autoPrerender: true,
  },
  reactStrictMode: true,
  swcMinify: true,
  autoPrerender: true
}

module.exports = nextConfig
